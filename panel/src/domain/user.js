/**
 * Склеивает имя и фамилию пользователя
 * @param {string} userName
 * @param {string} userLastName
 * @returns string
 */
export function getFullName(userName, userLastName) {
  return `${userName} ${userLastName}`
}
